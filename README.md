# Aws lambda Kotlin demo

This simple kotlin demo is for learning both kotlin and Lambda with JVM based languages.

## Build jar

To build lambda runnable jar file run following:

```
gradle shadowJar
```
